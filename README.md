改进提议
1. affiliate  ----registration, when a user place order, commission
2. order   ----order by the admin, order by the user
3. expiration  ----when expired, user plan will expire or not, user can submit extend plan request
4. generate proxy link ---residential proxy link, api link for ordered user
6. balance withdraw request ---- admin review and pay, user will be able to add crypto wallet address for withdraw
